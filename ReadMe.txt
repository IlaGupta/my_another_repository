
This is my first notepad file using Git.

And this will also make commit to save changes into repository

Steps to follow to add file to git repository::
In the git bash:
1/ Type git add ReadMe.txt
2. git commit = to make the changes commit.
3. git config --global user.name ""
   git config --global user.email "value"
given username and user.email
4. git commit -a -m "Your commit message eg. Added steps for doing commit to ReadMe file"

ghj